**第一优先级：端口号**

|协议|数字|
|---|---|
|HTTP|TCP 80|
|HTTPS|TCP 443|
|DNS|UDP 53|
|SMTP|TCP 25|
|POP3|TCP 110|
|IMAP|TCP 143|
|FTP 控制连接|TCP 21|
|FTP 数据连接|TCP 20 常见|

**HTTP 状态码**

| 状态码 | 含义                 |
| --- | ------------------ |
| 200 | OK，请求成功            |
| 301 | 永久重定向              |
| 304 | Not Modified，缓存未修改 |
| 400 | Bad Request        |
| 404 | Not Found          |
| 500 | 服务器内部错误            |
| 505 | HTTP 版本不支持         |

**层数和基本单位**

| 内容          | 数字            |
| ----------- | ------------- |
| OSI 模型      | 7 层           |
| TCP/IP 常用模型 | 5 层           |
| 1 Byte      | 8 bit         |
| IPv4 地址     | 32 bit，4 Byte |
| IPv4 每段范围   | 0-255         |
| MAC 地址      | 48 bit，6 Byte |
| IPv6 地址     | 128 bit       |
| IPv6 固定首部   | 40 Byte       |

**传输层**

|内容|数字|
|---|---|
|UDP 首部|8 Byte|
|UDP 首部字段|4 个字段，每个 2 Byte|
|TCP 首部最小长度|20 Byte|
|TCP 三次握手|3 次：SYN、SYN+ACK、ACK|
|TCP 四次挥手|4 次：FIN、ACK、FIN、ACK|
|快速重传|3 个冗余 ACK|
|SYN / FIN|各消耗 1 个序号|
|TCP 实际发送窗口|`min(cwnd, rwnd)`|

**拥塞控制**

|情况|数字规则|
|---|---|
|慢启动|每 RTT 近似翻倍：1、2、4、8...|
|拥塞避免|每 RTT 约 +1 MSS|
|超时|`ssthresh = cwnd/2`，`cwnd = 1 MSS`|
|3 个冗余 ACK|`ssthresh = cwnd/2`，`cwnd` 降到门限附近|

**IP / 分片 / 协议号**

|内容|数字|
|---|---|
|TCP 协议号|6|
|UDP 协议号|17|
|ICMP 协议号|1|
|TTL|每过 1 个路由器减 1|
|片偏移单位|8 Byte|
|DF=1|不允许分片|
|MF=1|后面还有分片|
|MF=0|最后一片|
|IP 首部校验和|只校验 IP 首部|

**子网速查**

|前缀|掩码|地址块大小|可用主机数|
|---|---|---|---|
|/24|255.255.255.0|256|254|
|/25|255.255.255.128|128|126|
|/26|255.255.255.192|64|62|
|/27|255.255.255.224|32|30|
|/28|255.255.255.240|16|14|
|/29|255.255.255.248|8|6|
|/30|255.255.255.252|4|2|

公式：

```
地址块大小 = 2^(主机位数)
可用主机数 = 2^(主机位数) - 2
主机位数 = 32 - 前缀长度
```

**路由协议**

|协议|数字/类型|
|---|---|
|RIP 最大有效跳数|15|
|RIP 中 16|不可达|
|OSPF|链路状态，Dijkstra|
|BGP|路径向量，AS 间|

**数据链路层**

|内容|数字|
|---|---|
|以太网最小帧长|64 Byte|
|CSMA/CD 争用槽|512 bit time|
|第 n 次碰撞 K 范围|`0 ~ 2^n - 1`|
|第 5 次碰撞可选 K 个数|32 个|
|第 5 次碰撞选 K=4 概率|`1/32`|
|10 Mbps 下 512 bit time|51.2 μs|
|100 Mbps 下 512 bit time|5.12 μs|

**CRC / GBN / SR**

|内容|数字规则|
|---|---|
|CRC 余数位数|`r = G 的位数 - 1`|
|CRC 发送前补 0|补 `r` 个 0|
|k bit 序号空间|`2^k`|
|GBN 最大窗口|`2^k - 1`|
|SR 最大窗口|`2^(k-1)`|

最该硬背的一行：  
**80、443、53、25、110、143、21、20；IPv4 32、IPv6 128/40、MAC 48、UDP 8、TCP 20、片偏移 8B、以太网最小 64B、CSMA/CD 512 bit time、RIP 15/16。**