## 题 1：关系代数

已知：

```
学生表(学号, 姓名, 所在院系)
成绩表(学号, 课程号, 成绩)
```

写关系代数：

```
查询选修 C801 的学生姓名。
```

```
π姓名(σ课程号='c801'(学生表⋈学生表.学号=成绩表.学号 成绩表) )
```

---

## 题 2：ER 转关系模式

一个教师可以讲授多门课程，一门课程也可以由多个教师讲授，联系属性为学时数。

请转换成关系模式，并指出主键和外键。

```
教师(Tno,Tname)
课程(Cno,Cname)
TC(Tno,Cno,学时数)

TC的主键为(Tno,Cno)
TC.Tno是外键，引用教师.Tno
TC.Cno是外键，引用课程.Cno
```

---

## 题 3：范式

已知：

```
R(学号, 课程号, 姓名, 课程名, 成绩)

F = {
  学号 → 姓名,
  课程号 → 课程名,
  (学号, 课程号) → 成绩
}
```

回答：

```
1. 候选键是什么？
2. 是否满足 2NF？为什么？
3. 如何分解到 2NF？
```

```
1. (学号, 课程号)
2.不满足，存在部分依赖
3.stu(学号，姓名)
cou(课程号，课程名)
gra(学号，课程号，成绩)
```
---

## 题 4：事务

简述事务的 ACID。
```
A：原子性，事务中的操作要么全部做，要么全部不做
C：一致性，事务执行前后数据库要保持一致
I：隔离性
D：持久性
```

---

## 题 5：并发控制

判断：

```
所有事务都遵守两段锁协议，则一定不会发生死锁。
```